name: Docker

on:
  push:
    branches: [ master ]

env:
  IMAGE_NAME: departments-api

jobs:
  build:
    name: Build container image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      # - name: Get tag
      #   id: tag
      #   uses: dawidd6/action-get-tag@v1
      - name: Log into registry
        uses: docker/login-action@v1
        with:
          registry: ${{ secrets.ACR_ENDPOINT }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}
      - name: Build & Push
        uses: docker/build-push-action@v2
        with:
          push: true
          build-args: |
            version=${{ steps.tag.outputs.tag }}
          tags: ${{ secrets.ACR_ENDPOINT }}/${{ env.IMAGE_NAME }}:${{ github.run_attempt }}
     
      - uses: Azure/login@v1
        with:
         creds:
            '{"clientId":"5c023096-5c14-43b1-acae-f0f22f115f56","clientSecret":"grY8Q~PDZ71PaveGw448ZbKhGzCHvja4x~Jtla5E","subscriptionId":"0db1b169-34da-432c-98d4-2db3188443a7","tenantId":"a787b852-29bf-4164-add5-7d55cac58f45"}'
  
      - name: 'Run Azure CLI commands'
        run: |
          az account show
          az group list
          pwd 